```
#与Java的不同点

1. scala用scalac进行编译，兼容java库，即面向对象也面向函数
1. 流程控制结构中，条件表达式要有返回值
2. scala中没有三目表达式，可以使用条件表达式替代
4. break和continue的使用变了，使用break需要饮用Breaks包，调用breakable包裹for循环，而continue是将breakable使用在循环之中
5. 惰性常量变量以及方法，对标java中的懒汉式
```

